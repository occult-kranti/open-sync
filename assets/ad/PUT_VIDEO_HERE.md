# Put the video here

This folder should contain:

- `open-sync-ad.mp4` — the 88-second product film (720p, ~15 MB)
- `poster.png` — optional poster frame

The README's `<video>` tag and download link already point at
`assets/ad/open-sync-ad.mp4`. Upload the mp4 via **Add file → Upload files**
in the GitHub web UI (the 25 MB limit fits), or push it with git over HTTPS.
